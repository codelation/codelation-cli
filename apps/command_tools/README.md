# CommandTools

**TODO: Add description**

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed as:

  1. Add `command_tools` to your list of dependencies in `mix.exs`:

    ```elixir
    def deps do
      [{:command_tools, "~> 0.1.0"}]
    end
    ```

  2. Ensure `command_tools` is started before your application:

    ```elixir
    def application do
      [applications: [:command_tools]]
    end
    ```

